export class User {
    id: string;
    username: string;
    email: string;
    password: string;
    hasProfilePicture: boolean;
}

export class FavoritePokemon {
    id: string
    favoritePokemonId: string;
}